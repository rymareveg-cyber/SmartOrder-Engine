# SmartOrder Engine

AI-система для автоматической обработки заказов в e-commerce с интеграцией 1С:Управление небольшой фирмой.

## 🚀 Быстрый старт

### Этап 1: Синхронизация каталога 1С

#### Требования
- Python 3.11+
- PostgreSQL 14+
- Redis 7+
- 1С:Управление небольшой фирмой с настроенным HTTP Service

#### Установка

1. **Клонирование и установка зависимостей:**
```bash
pip install -r requirements.txt
```

2. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

3. **Создание базы данных:**
```bash
# Подключитесь к PostgreSQL и создайте базу данных
createdb smartorder

# Примените миграции
psql -d smartorder -f database/migrations/001_create_catalog_tables.sql
```

4. **Включение расширения для полнотекстового поиска (опционально):**
```sql
-- В PostgreSQL выполните:
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

#### Запуск

**Синхронизация каталога (с планировщиком):**
```bash
python execution/sync_1c_catalog.py
```

**Однократная синхронизация:**
```bash
python execution/sync_1c_catalog.py --once
```

**API сервер:**
```bash
python execution/api_catalog_server.py
# Или через uvicorn:
uvicorn execution.api_catalog_server:app --host 0.0.0.0 --port 8025
```

#### Проверка работы

**Health check:**
```bash
curl http://localhost:8025/health
```

**Получить каталог:**
```bash
curl http://localhost:8025/api/catalog
```

**Поиск товаров:**
```bash
curl "http://localhost:8025/api/catalog/search?q=варочная"
```

**Товар по артикулу:**
```bash
curl http://localhost:8025/api/catalog/ФР-00000044
```

## 📁 Структура проекта

```
SmartOrder Engine/
├── directives/          # Директивы (SOP) для агента
│   └── 1c_catalog_sync.md
├── execution/           # Python скрипты (детерминированные инструменты)
│   ├── sync_1c_catalog.py
│   └── api_catalog_server.py
├── database/
│   └── migrations/      # SQL миграции
│       └── 001_create_catalog_tables.sql
├── docs/                # Документация проекта
├── .env                 # Переменные окружения (не коммитить!)
├── .env.example         # Пример переменных окружения
├── requirements.txt     # Зависимости Python
├── AGENTS.md            # Инструкции для агента
└── project.md           # Описание проекта
```

## 📚 Документация

Полная документация находится в папке `docs/`:
- `Step-by-Step Guide.md` - Пошаговое руководство по реализации
- `Technical Specification.md` - Техническое задание
- `API Documentation.md` - Документация API
- `Agent Benefits.md` - Преимущества архитектуры
- `Universal 1C HTTP API Bridge.md` - Описание интеграции с 1С

## 🔧 Архитектура

Проект использует 3-уровневую архитектуру (см. `AGENTS.md`):
- **Уровень 1: Директива** (`directives/`) - SOP в Markdown
- **Уровень 2: Оркестрация** - LLM-агент (принятие решений)
- **Уровень 3: Выполнение** (`execution/`) - Детерминированные Python-скрипты

## 📝 Логи

Логи синхронизации сохраняются в `logs/catalog_sync.log`

## ⚙️ Настройка

Все настройки выполняются через переменные окружения в файле `.env`. См. `.env.example` для примера.

## 🐛 Устранение неполадок

### Проблема: Ошибка подключения к 1С
- Проверьте `ONEC_BASE_URL`, `ONEC_USERNAME`, `ONEC_PASSWORD`
- Убедитесь, что 1С HTTP Service запущен и доступен
- Проверьте настройки файрвола

### Проблема: Ошибка подключения к PostgreSQL
- Проверьте `DATABASE_URL`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя БД

### Проблема: Ошибка подключения к Redis
- Redis опционален (кэширование)
- Если Redis недоступен, кэширование отключается автоматически
- Проверьте `REDIS_URL` если используете кэш

## 🚀 Деплой в Dokploy

Проект настроен для автоматического деплоя в Dokploy через GitHub.

### Требования
- GitHub репозиторий: https://github.com/rymareveg-cyber/SmartOrder-Engine.git
- Dokploy установлен на VPS: https://dokploy.rymarev.ru/

### Быстрый старт

1. Убедитесь, что все файлы конфигурации (Dockerfile, .dockerignore) в репозитории
2. Подключите репозиторий в Dokploy
3. Настройте переменные окружения
4. Запустите деплой

**Подробная инструкция**: см. `DEPLOY.md`

### Важно
- Папка `docs/` автоматически исключается из Docker образа
- Все секретные данные настраиваются в Dokploy через переменные окружения
- При каждом push в GitHub происходит автоматический пересбор и перезапуск (если настроен auto-deploy)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/catalog_sync.log`
2. Проверьте health check endpoint: `/health`
3. Убедитесь, что все переменные окружения настроены правильно
4. Для проблем с деплоем см. `DEPLOY.md`